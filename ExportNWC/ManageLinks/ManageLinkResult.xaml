<UserControl x:Class="NewJeans.ManageLinkResult"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Background="#2C2F38" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- 리스트 영역 -->
            <RowDefinition Height="Auto"/>
            <!-- ProgressBar -->
            <RowDefinition Height="Auto"/>
            <!-- 경로 버튼 -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 리스트 영역 -->
        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <CheckBox x:Name="chkSelectAll"
                      Grid.Row="0"
                      Content="전체 선택"
                      Foreground="White"
                      Checked="chkSelectAll_Checked"
                      Unchecked="chkSelectAll_Unchecked"/>

            <ListView x:Name="RevitLinkList"
                      Grid.Row="1"
                      ItemsSource="{Binding RevitLinkItems}"
                      Background="#3B3F48"
                      BorderBrush="#5A5D66"
                      BorderThickness="1"
                      Foreground="White"
                      Margin="0,5,0,0"
                      SelectionMode="Extended"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <!-- 기본 배경 투명 -->
                        <Setter Property="Background" Value="#2C2F38"/>
                        <Setter Property="BorderThickness" Value="0"/>

                        <!-- 호버 시 배경 투명 -->
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF565B6B"/>
                            </Trigger>

                            <!-- 선택 시 배경 투명 -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF565B6B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="선택" Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="링크 이름" Width="180" DisplayMemberBinding="{Binding Name}" />

                        <GridViewColumn Header="상태" Width="80" DisplayMemberBinding="{Binding Status}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <!-- 진행바 -->
        <ProgressBar x:Name="progressBar"
                     Grid.Row="1" Grid.ColumnSpan="2"
                     Height="20" 
                     Margin="10,10,10,10"
                     Foreground="#FFF99D5D"
                     Background="#3B3F48"
                     Minimum="0" Maximum="100"
                     Visibility="Collapsed"/>

        <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="10" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Load 버튼 -->
            <Button x:Name="btnLoad"
            Content="Load"
            Width="80"
            Height="30"
            Background="#3B3F48"
            Foreground="White"
            FontWeight="Bold"
            Grid.Column="0"
            Margin="0,0,10,0"
            Click="btnLoad_Click"/>

            <!-- Unload 버튼 -->
            <Button x:Name="btnUnload"
            Content="Unload"
            Width="80"
            Height="30"
            Background="#3B3F48"
            Foreground="White"
            FontWeight="Bold"
            Grid.Column="1"
            Click="btnUnload_Click"/>
        </Grid>

    </Grid>
</UserControl>
